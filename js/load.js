
//Load common code that includes config, then load the app logic for this page.
require(['./config'], function (config) {
  //require(['jsx!app/menu/main_menu']);
  require(['jsx!app/menu/app_menu']);
});
